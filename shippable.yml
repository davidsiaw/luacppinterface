language: python

before_script:
  - mkdir -p shippable/codecoverage

before_install:
  - sudo pip install gcovr
  - sudo pip install cpp-coveralls --use-mirrors

script: ./build-travis.sh

after_success:
  - ls
  - gcovr -r . --xml
  - cp *.xml shippable/codecoverage
